import { content } from './content'

export const interviewQA = {
  intro: [
    { q: "What is Git and how is it different from other VCS?", a: "Git is a distributed VCS — every developer has a full copy of the repo including history. Unlike centralized VCS (SVN), you can work offline, branch freely, and merge without a central server. Git is faster, more flexible, and supports non-linear development.", level: "basic" },
    { q: "Explain the Git object model.", a: "Git has 4 object types: Blob (raw file content, addressed by SHA-1), Tree (directory structure, contains pointers to blobs/trees), Commit (snapshot — points to a tree, parent commit, has metadata: author, msg, timestamp), Tag (named pointer to a commit for releases). Everything is content-addressable.", level: "intermediate" },
    { q: "What is HEAD in Git?", a: "HEAD is a pointer to the currently checked-out commit/branch. Usually it points to a branch ref (like refs/heads/main), which in turn points to the latest commit. In 'detached HEAD' state, it points directly to a commit hash, not a branch.", level: "intermediate" },
    { q: "What is the difference between git fetch and git pull?", a: "git fetch downloads remote changes to your local remote-tracking branches (origin/main) but doesn't modify your working branch. git pull = git fetch + git merge. Prefer fetch when you want to review changes before integrating.", level: "basic" },
    { q: "Explain distributed vs centralized version control.", a: "CVCS (SVN): single central server, limited offline work, single point of failure. DVCS (Git): every clone is a full backup, full offline capability, flexible branching, independent collaboration. Git is better for open source and remote teams.", level: "basic" },
  ],
  basics: [
    { q: "What does git add do internally?", a: "git add creates a blob object from the file content, compresses and stores it in .git/objects, then updates the index (staging area) with the blob's SHA-1 hash. It creates a snapshot of the file at that moment — not just a reference.", level: "intermediate" },
    { q: "What is the staging area and why does it exist?", a: "The staging area (index) is a middle layer between working directory and repo. It lets you craft commits precisely — stage only what belongs in one logical change. This enables clean, atomic commits rather than committing everything at once.", level: "basic" },
    { q: "What does git commit store?", a: "A commit stores: a pointer to a tree object (directory snapshot), pointer(s) to parent commit(s), author name/email/timestamp, committer name/email/timestamp, and the commit message. The SHA-1 hash is computed from all this content — changing any field changes the hash.", level: "intermediate" },
    { q: "How does .gitignore work?", a: "When git add executes, it checks .gitignore rules against each file. If matched, the file is not staged. Rules are pattern-based: wildcards (*), negation (!), directory-specific (trailing /). Files already tracked before adding to .gitignore need git rm --cached.", level: "basic" },
    { q: "What is git init and what does it create?", a: "git init creates a .git directory with: HEAD (pointer to current branch), config (repo settings), description, hooks/ (event scripts), info/, objects/ (all git objects), refs/ (branches and tags). This turns any directory into a Git repo.", level: "basic" },
    { q: "What is the difference between git clone --depth 1 and a full clone?", a: "Shallow clone (--depth 1) only downloads the latest snapshot without history. Useful for CI/CD or when you only need the latest code and want faster clone. A full clone has complete history enabling git log, git blame, git bisect.", level: "intermediate" },
  ],
  history: [
    { q: "What is git bisect and when would you use it?", a: "git bisect uses binary search algorithm through commit history to find which commit introduced a bug. You mark one commit as good, one as bad, and Git checks out the midpoint — you test, mark good/bad, repeat. Halves the search space each time — O(log n) efficiency.", level: "advanced" },
    { q: "How do you find who last modified a specific line of code?", a: "git blame <file> — shows each line annotated with: commit hash, author name, timestamp, and line content. Use -w to ignore whitespace changes. Combine with git show <hash> to see the full commit context.", level: "basic" },
    { q: "What is the difference between git diff, git diff --cached, and git diff HEAD?", a: "git diff: working dir vs staging area (unstaged changes). git diff --cached: staging area vs last commit (what will be committed). git diff HEAD: working dir vs last commit (all changes since last commit, staged + unstaged).", level: "intermediate" },
    { q: "How do you search for a specific string across all commits?", a: "git grep 'pattern' searches working dir. git log -S 'pattern' (pickaxe) finds commits where the count of that string changed. git log --grep='pattern' searches commit messages. git grep 'pattern' $(git rev-list --all) searches all history.", level: "advanced" },
    { q: "What does git log --graph show?", a: "It renders an ASCII art graph of the commit history showing branches and merges. Combined with --oneline --decorate, you get a compact visual of the entire branch topology — very useful for understanding repo history and reviewing merges.", level: "basic" },
  ],
  files: [
    { q: "What is the difference between git rm and just deleting a file?", a: "Deleting a file manually only removes it from the working directory — git still tracks it as 'deleted' but unstaged. git rm removes the file AND stages the deletion in one step. git rm --cached removes from tracking only, keeps the file locally.", level: "basic" },
    { q: "Why use git mv instead of mv + git add?", a: "git mv moves/renames the file AND updates the git index atomically. It keeps git's rename detection clean. Technically, git mv = mv + git rm + git add, but it's cleaner. Use git log --follow to track file history across renames.", level: "intermediate" },
    { q: "What are file permissions in Git?", a: "Git tracks the executable bit only — not full Unix permissions. A file is either regular (100644) or executable (100755). Git doesn't track read/write permissions. On Windows, executable bit doesn't exist natively — use .gitattributes to manage cross-platform.", level: "intermediate" },
    { q: "What is .gitattributes and when do you need it?", a: ".gitattributes specifies attributes per file pattern: line ending normalization (eol=lf/crlf), merge strategies, diff drivers, binary vs text treatment. Critical for cross-platform teams — prevents line ending issues that corrupt diffs.", level: "advanced" },
  ],
  undoing: [
    { q: "What is the difference between git revert and git reset?", a: "git reset moves HEAD backward — rewrites history (dangerous on shared branches). git revert creates a new commit that undoes a previous commit — preserves history, safe for shared branches. Use reset for local unpushed work, revert for shared/public history.", level: "intermediate" },
    { q: "Explain the 3 modes of git reset.", a: "Soft: moves HEAD only — changes remain staged. Mixed (default): moves HEAD + unstages changes — changes in working dir. Hard: moves HEAD + clears staging + reverts working dir — destructive, can lose work. Recover with git reflog.", level: "intermediate" },
    { q: "What is git reflog?", a: "Reflog records every movement of HEAD — even after reset, rebase, or branch deletion. It's your safety net. git reflog shows hash + action. Use git reset --hard <hash> to recover from accidental hard resets. Reflog entries expire after 90 days by default.", level: "advanced" },
    { q: "When would you use git stash?", a: "When you need to switch branches with uncommitted changes you're not ready to commit. git stash saves modified tracked files + staged changes to a stack. git stash -u also stashes untracked files. git stash pop applies and removes the latest stash.", level: "basic" },
    { q: "What is the difference between git restore and git checkout for files?", a: "git restore was introduced in Git 2.23 to split git checkout's overloaded functionality. git restore <file> restores working dir. git restore --staged <file> unstages. git checkout does both but also switches branches — confusing. Prefer restore for clarity.", level: "intermediate" },
    { q: "How do you undo a commit that was already pushed?", a: "Use git revert <hash> — it creates a new commit undoing the changes, preserving history. Never use git reset on pushed commits shared with others — it rewrites history and causes conflicts for teammates who've pulled. If absolutely needed, coordinate with team first.", level: "intermediate" },
  ],
  branches: [
    { q: "Explain the difference between git branch and git switch/checkout.", a: "git branch creates, lists, or deletes branch refs. It does not change HEAD. git switch (or git checkout) changes which branch is checked out (updates HEAD and working tree). Use 'git switch -c <name>' to create+switch in one step.", level: "basic" },
    { q: "What happens when you delete a branch locally and remotely?", a: "Locally: git branch -d removes the ref if merged; -D forces removal. Remotely: git push origin --delete <name> removes remote ref. Other clones still have the commits until garbage-collected; use reflog to recover local refs if needed.", level: "intermediate" },
    { q: "How do you safely create and publish a feature branch for collaboration?", a: "Create locally (git switch -c feature/name), work & commit, push with upstream (git push -u origin feature/name). Open a PR for review. Keep branch small and rebase onto latest main when needed to reduce merge conflicts.", level: "basic" },
    { q: "Scenario: you need to switch branches but have uncommitted changes. What are your options?", a: "Options: git stash (save changes), git commit to temporary WIP commit, or use git switch - (if changes do not conflict). Prefer stash for temporary work; remember to pop or apply later.", level: "intermediate" },
    { q: "What is a detached HEAD and how do you recover from it?", a: "Detached HEAD happens when you checkout a commit/hash directly. You're not on a branch, so commits may be orphaned. Recover by creating a branch at the detached commit: git switch -c temp-branch, or merge/cherry-pick the commits into a branch before switching away.", level: "intermediate" },
    { q: "When should you rebase a feature branch vs merge it?", a: "Rebase to keep a linear history and integrate upstream changes onto your branch before merging. Merge (no-rebase) preserves true topology and is safer for long-lived branches or when multiple collaborators share the branch. Never rebase shared public branches without coordination.", level: "intermediate" },
    { q: "Scenario: two developers pushed diverging commits to the same branch. How do you resolve this?", a: "Options: (1) Pull with rebase (git pull --rebase) to replay local commits on top of remote; resolve conflicts and push. (2) Merge remote into local (git fetch && git merge origin/branch) and resolve conflicts. Communicate with teammates before force-pushing.", level: "intermediate" },
    { q: "How do you clean up remote-tracking refs for branches that were deleted upstream?", a: "Use git fetch --prune or git remote prune origin to remove stale origin/* refs locally. You can also run git fetch -p as a convenience. This keeps local branch lists accurate.", level: "basic" },
    { q: "Describe a branching strategy for a medium-sized team (features, releases, hotfixes).", a: "Use short-lived feature branches (feature/*) for work. Keep a long-lived main branch (main) and a develop/release branch if needed. Use release branches for preparing releases and hotfix branches off main for urgent fixes. Protect main with branch protections and require PR reviews.", level: "intermediate" },
    { q: "Scenario: you accidentally committed sensitive data to a feature branch and pushed it. How do you handle it?", a: "Rotate the leaked credentials first. Then remove the data from history: use git filter-repo (or BFG) to scrub, force-push cleaned history (git push --force-with-lease), and notify collaborators. Consider repository rotation if secrets were widely exposed.", level: "advanced" },
    { q: "What are the trade-offs of long-lived vs short-lived branches?", a: "Long-lived branches reduce context switching but increase merge conflicts and integration drift. Short-lived branches encourage small PRs and faster integration but require more coordination. Prefer short-lived feature branches with frequent integration for CI-driven teams.", level: "intermediate" },
  ],
  merge: [
    { q: "Explain fast-forward vs three-way merge.", a: "Fast-forward: target branch pointer simply moves forward to the source tip when no divergent commits exist (no merge commit). Three-way merge creates a new merge commit that has two parents and records the integration when branches have diverged.", level: "basic" },
    { q: "When and why would you use --no-ff when merging?", a: "Use --no-ff to force a merge commit even for fast-forwardable merges. This preserves an explicit record of the feature branch integration and groups related commits together for historical clarity.", level: "intermediate" },
    { q: "Scenario: merge conflict with many files — how do you approach resolving it?", a: "Break it down: run git status to list conflicts, open the most critical files first, use a merge tool (meld/vimdiff), run tests frequently, and prefer smaller logical resolutions. Use git add to mark resolved files and git commit to finish the merge. Communicate with original authors if needed.", level: "intermediate" },
    { q: "What is an octopus merge and when is it used?", a: "Octopus merge merges more than two branches at once. It's useful for merging many trivial branches into one (e.g., release orchestration) and will fail if conflicts are complex — it's best for non-overlapping changes.", level: "advanced" },
    { q: "How do you revert a merge commit?", a: "Use git revert -m <parent-number> <merge-commit-hash>. Choose the parent you want to keep as mainline (usually 1). This creates a new commit that undoes the effects of the merge without rewriting history.", level: "intermediate" },
    { q: "Explain rebase --interactive and a common use-case.", a: "git rebase -i allows editing, squashing, reordering, or dropping commits. Common use-case: clean up a messy feature branch before merging — squash small fixup commits into meaningful changes and reword commit messages for clarity.", level: "advanced" },
    { q: "Scenario: you rebased a branch and now remote has the old commits — what next?", a: "If the branch was only local, push normally. If you already pushed before rebasing, you'll need to force-push (git push --force-with-lease) after confirming collaborators won't be disrupted. Communicate clearly and prefer --force-with-lease.", level: "advanced" },
    { q: "What are conflict markers and how do you interpret them?", a: "Conflict markers look like <<<<<<<, =======, >>>>>>> showing your HEAD changes, separator, and incoming branch changes. Keep desired parts, remove markers, run tests, then git add the file to mark resolved.", level: "basic" },
    { q: "Describe a safe workflow to integrate a long-running feature branch into main with minimal disruption.", a: "Keep branch rebased onto main regularly or merge main into the branch frequently, run CI on PRs, split big features into smaller mergeable chunks, use feature toggles for incomplete work, and perform a staged release with feature branch testing before merging.", level: "advanced" },
    { q: "When should you prefer squash merges?", a: "Squash when you want a single clean commit per feature in the target branch (e.g., maintainers prefer concise history). It hides intermediate WIP commits but loses per-commit granularity on the feature branch.", level: "intermediate" },
  ],
  remote: [
    { q: "What are common authentication methods for remotes and their trade-offs?", a: "HTTPS + token: easy to set up, good for CI; SSH keys: secure and convenient for developer workflows; credential helpers cache tokens/credentials. Use SSH for developer access and tokens for automation where SSH isn't suitable.", level: "basic" },
    { q: "Explain git fetch vs git pull and why you might prefer fetch in a team.", a: "git fetch updates remote-tracking branches without modifying working branches; git pull fetches and merges (or rebases). Prefer fetch to inspect incoming changes before integrating to avoid unexpected merges and to review with CI.", level: "basic" },
    { q: "Scenario: your local branch has diverged from origin and push is rejected. How do you reconcile?", a: "Fetch remote, inspect changes (git log HEAD..origin/branch), rebase local commits on top of origin (git pull --rebase) or merge origin into local, fix conflicts, then push. If rewriting history, use --force-with-lease with caution.", level: "intermediate" },
    { q: "What does --force-with-lease protect against?", a: "--force-with-lease ensures you only force-push if the remote ref matches what you expect (i.e., no new commits were added by others). It reduces risk compared to plain --force by preventing overwriting others' work accidentally.", level: "intermediate" },
    { q: "How do you add and work with multiple remotes?", a: "Use git remote add <name> <url> for each remote (e.g., origin, upstream). Fetch from different remotes: git fetch upstream; merge or cherry-pick specific commits. Useful in forks: keep upstream in sync and push to origin.", level: "intermediate" },
    { q: "Scenario: you need to rename a remote URL (server migration). How do you switch without losing config?", a: "Use git remote set-url origin <new-url> to update the URL while preserving fetch/push settings. Optionally verify with git remote -v and test fetch/push.", level: "basic" },
    { q: "What is git remote prune origin and when should you run it?", a: "Prunes stale remote-tracking branches that were deleted on the remote. Run after cleaning up branches upstream to keep your local view in sync: git fetch --prune or git remote prune origin.", level: "basic" },
    { q: "How do you debug failed authentication or permission issues when pushing?", a: "Check remote URL (git remote -v), test connectivity (ssh -T git@host or curl for HTTPS), clear cached credentials or update token, check access rights on remote (repo permissions), and inspect credential helper logs if present.", level: "intermediate" },
    { q: "Explain workflows that involve forks and upstream repositories (open-source model).", a: "Fork repository to your account (origin), add upstream remote pointing to original repo, work on feature branches, push to your origin, open PR against upstream, periodically fetch & rebase from upstream to stay current.", level: "basic" },
    { q: "Scenario: large file accidentally committed and pushed. What do you do?", a: "Remove the file from history using git filter-repo or BFG, add the file to .gitignore, force-push cleaned history (notify collaborators), and consider using Git LFS for large files going forward.", level: "advanced" },
  ],
  github: [
    { q: "Describe the fork & PR workflow for open-source contributions.", a: "Fork the upstream repo, clone your fork, create a feature branch, implement changes, push branch to your fork, create a PR from your fork to upstream, address review feedback, and merge when approved.", level: "basic" },
    { q: "How do you keep your fork in sync with the upstream repo?", a: "Add the original repository as an upstream remote (git remote add upstream <url>), fetch upstream (git fetch upstream), then rebase or merge upstream/main into your branch (git rebase upstream/main) before pushing to your fork.", level: "intermediate" },
    { q: "What are branch protection rules and why use them?", a: "Branch protection rules enforce policies on branches (require PR reviews, required status checks, disallow force pushes). They prevent accidental direct pushes and ensure CI and code review before merging to main.", level: "basic" },
    { q: "When should you use GitHub Actions vs external CI?", a: "Use GitHub Actions for tight integration with GitHub events (PRs, pushes) and when you want simple workflows stored in the repo. External CI may be used for organization-specific tooling or if you already have an established CI provider with special capabilities.", level: "intermediate" },
  ],
  advanced: [
    { q: 'What are packfiles and why do they matter?', a: `Packfiles bundle many loose Git objects into compressed files and store deltas between objects. They reduce disk usage and speed up clone/fetch operations. Commands: git gc, git repack -a -d --depth=250 --window=250 to optimize packs; git verify-pack to inspect pack contents.`, level: 'advanced' },
    { q: 'When should you run git gc or repack?', a: `Run git gc (garbage collect) when the repository has many loose objects or after large history rewrites. Use git gc --aggressive or git repack for tighter packing when clones become slow or disk usage is high.`, level: 'advanced' },
    { q: 'How do annotated and lightweight tags differ and how do you manage tags?', a: `Annotated tags store metadata (tagger, date, message) and are created with git tag -a v1.0.0 -m "msg"; lightweight tags are simple refs. List tags with git tag, view details with git show <tag>, and push tags with git push origin --tags.`, level: 'advanced' },
    { q: 'What is cherry-picking and how do you handle conflicts?', a: `Cherry-pick applies selected commits from another branch onto your current branch (git cherry-pick <hash>). For multiple commits you can specify ranges. If conflicts occur resolve them, git add the files, then git cherry-pick --continue or abort with --abort.`, level: 'advanced' },
    { q: 'When should you use git filter-repo vs git filter-branch?', a: `git filter-branch is older and slow for large repos. Prefer git-filter-repo (pip install git-filter-repo) for safe, fast history rewrites (e.g., removing secrets). Always coordinate and force-push cleaned history afterwards.`, level: 'advanced' },
    { q: 'What are Git worktrees and when are they useful?', a: `git worktree lets you check out multiple branches simultaneously in the same repository (git worktree add ../branch <branch>). Useful for working on long-lived branches, running tests in parallel, or preparing a release without switching your main working tree.`, level: 'advanced' },
    { q: 'How do submodules/subtrees differ and when to use them?', a: `Submodules embed a reference to another repo (git submodule add <url>) and keep it separate; subtrees merge another repo's contents into a subdirectory. Use submodules when you need clear separation and independent history; use subtree when you want integrated content without the separate clone workflow.`, level: 'advanced' },
    { q: 'How do you safely remove sensitive data from history?', a: `Rotate credentials first. Use git-filter-repo (or BFG) to remove files from all history, then force-push the cleaned branches (git push --force-with-lease). Notify collaborators and consider repository rotation if exposure was wide.`, level: 'advanced' },
    { q: 'What tooling helps manage client-side hooks in JS projects?', a: `Use tooling like Husky to manage Git hooks (pre-commit, commit-msg, pre-push). Install and configure hooks to run linters, formatters, or tests automatically before commits or pushes.`, level: 'intermediate' },
  ],
}

export const quickFacts = {
  intro: ["SHA-1 produces a 40-character hex hash", "Git stores content not diffs — every commit is a full snapshot", "Linus Torvalds created Git in 2005 for Linux kernel development", ".git directory IS the repository — copy it = clone the repo"],
  basics: ["git add . stages all, git add -p stages interactively (patch mode)", "Amend only before pushing — amend rewrites commit hash", "git commit --dry-run shows what would be committed without committing", "Empty commit: git commit --allow-empty -m 'msg'"],
  history: ["git log --all shows commits on ALL branches, not just current", "git blame -C detects code moved from other files", "git bisect can be automated with --exec flag + test script", "git shortlog -sn gives contributor leaderboard"],
  files: ["git rm -r removes directories recursively", "git mv is case-sensitive even on case-insensitive macOS", "Track executable bit: git update-index --chmod=+x file", ".gitkeep is a convention to track empty directories"],
  undoing: ["git reflog expires after 90 days by default", "git stash creates a special stash commit object", "git clean -n (dry run) before git clean -f (actual delete)", "git restore is preferred over git checkout for file operations in modern Git"],
  branches: ["Branch names live in .git/refs/heads/", "Deleting a branch does not delete commits if reachable from other refs", "Use prefixes like feature/ or bugfix/ for clarity"],
  merge: ["Fast-forward moves branch pointer without merge commit", "Rebase rewrites commits — avoid on shared branches", "git rerere helps reuse conflict resolutions"],
  remote: ["Origin is just a convention for remote name", "Use --force-with-lease instead of --force when pushing rewritten history", "git fetch updates remote-tracking branches (origin/*)"]
  ,
  advanced: [
    'Packfiles compress many loose objects — run `git gc` to pack and save space',
    'Use `git repack -a -d --depth=250 --window=250` for tighter packing on large repos',
    'Annotated tags store metadata (use `git tag -a`) — push tags with `git push origin --tags`',
    'Use `git cherry-pick` to apply individual commits; resolve conflicts then `git cherry-pick --continue`',
    'Prefer `git-filter-repo` over `git filter-branch` for rewrites (remove secrets safely)',
    'Use `git worktree` to check out multiple branches concurrently for parallel work',
    'Submodules keep separate repo history; subtrees embed another repo — choose based on coupling',
    'Use Husky to manage client-side hooks (pre-commit, commit-msg, pre-push) in JS projects',
  ],
  github: [
    "Use PRs for code review and CI checks",
    "Protect main with required status checks and reviews",
    "Use Actions for CI and automated workflows stored in the repo",
  ]
}
